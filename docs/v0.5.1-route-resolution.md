# v0.5.1 Route Resolution

> Aletheia Nexus v0.5.1 turns scholarly landing routes into explainable file candidates.

**Version:** 0.5.1

**Layer:** Route Resolution（访问路径解析）

**Status:** FINALIZED / FROZEN FOR MERGE

## 1. Goal

v0.4 Discovery answers:

> Where might full text exist?

v0.5.0 Direct PDF Acquisition answers:

> If I already have a PDF candidate, can I retrieve it safely and verify that it is the target article?

v0.5.1 fills the gap between them:

```text
FullTextCandidate
(landing / resolver / repository / unknown route)
↓
safe page retrieval
↓
page semantics + identity evidence
↓
generic scholarly HTML signals
↓
DerivedFullTextCandidate[]
↓
v0.5.0 Direct PDF Acquisition + Validation
```

The layer does **not** claim that a derived URL is a valid PDF. It only makes the
route more concrete while preserving why and where the route was derived.

The central invariant is:

> **Derived Candidate ≠ Verified File.**

## 2. Architectural boundary

The v0.5 acquisition line is deliberately split into distinct responsibilities:

```text
v0.5.0  Direct PDF Acquisition + Validation
        one concrete PDF candidate → trustworthy file result

v0.5.1  Route Resolution
        one route/page → zero or more explainable PDF candidates

v0.5.2  Multi-route Acquisition
        coordinate many provider + derived routes until VERIFIED

later   Authenticated / Browser Acquisition
        SSO / CARSI / browser state / Human-in-the-loop
```

This separation is intentional. v0.5.1 does not become a hidden multi-route
downloader, and v0.5.0 does not accumulate publisher-page parsing logic.

## 3. Public API

```python
from aletheia_nexus.acquire.fulltext import resolve_full_text_route

result = resolve_full_text_route(
    candidate,
    expected_title="Optional known title",
)

print(result.status)
for derived in result.candidates:
    print(derived.candidate.url)
    print(derived.method)
    print(derived.role_hint)
    print(derived.evidence)
```

The result is a `RouteResolutionResult` containing:

```text
source_candidate
status
candidates
page
page_type
identity
error
attempts
elapsed_seconds
```

Each `DerivedFullTextCandidate` keeps:

```text
candidate           normalized PDF-shaped FullTextCandidate
parent_url          route that led to derivation
source_page_url     actual page used for extraction
derivation method   why the URL was produced
role_hint           ARTICLE / SUPPLEMENT / UNKNOWN
evidence            human-readable derivation evidence
priority            deterministic acquisition-oriented priority
```

Provider provenance and AN derivation provenance remain separate. A URL derived by AN
from an OpenAlex landing page is not falsely represented as though OpenAlex directly
reported that PDF URL.

## 4. Resolution statuses

```text
RESOLVED
NO_FILE_CANDIDATES
PAGE_MISMATCH
AUTH_REQUIRED
ACCESS_BLOCKED
NOT_FOUND
TOO_LARGE
UNSAFE_URL
REDIRECT_ERROR
REQUEST_ERROR
NETWORK_ERROR
RATE_LIMITED
SERVICE_ERROR
INVALID_CONTENT
ERROR
```

Important semantics:

```text
401 → AUTH_REQUIRED
403 → ACCESS_BLOCKED
```

HTTP 403 alone is insufficient evidence that login or institutional authorization is
the cause. It can also represent WAF（Web 应用防火墙）, anti-bot, IP policy, or other
server-side blocking.

`NO_FILE_CANDIDATES` is not an exception. It means the route was handled but the current
response did not expose a concrete file route through evidence v0.5.1 is designed to
trust.

## 5. Safe page retrieval

Route pages reuse the v0.5 acquisition safety boundary:

- HTTP(S) only;
- hostname required;
- embedded credentials rejected;
- non-global/private/loopback/link-local literal IPs rejected;
- best-effort DNS preflight safety checks;
- redirects followed manually and every target revalidated;
- bounded redirect count;
- bounded timeout;
- bounded HTML/text body size;
- network/rate-limit/service errors keep explicit retry semantics.

Defaults:

```text
page timeout       30 s
page size          10 MiB
redirect limit      8
read chunk         64 KiB
```

### Binary/PDF sniffing

A route advertised as a landing page can actually return PDF bytes directly. v0.5.1
therefore examines a bounded response prefix for the `%PDF-` marker.

The transport explicitly reads in 64 KiB chunks. This keeps the contract honest: when a
landing route unexpectedly returns a PDF, Route Resolution does not silently consume the
whole paper before handing the concrete route to v0.5.0.

A large response with an uncertain binary Content-Type such as
`application/octet-stream` is not rejected from `Content-Length` alone before PDF
sniffing. Definite large HTML/text responses can still be rejected early.

If a PDF response is detected, v0.5.1 returns a `DIRECT_PDF_RESPONSE` candidate. Actual
file retrieval and validation still belong to v0.5.0.

## 6. Generic HTML evidence

v0.5.1 deliberately prefers standards and page semantics over a publisher-domain map.

Supported generic signals include:

### Scholarly metadata

```text
citation_pdf_url
bepress_citation_pdf_url
eprints.document_url
pdf_url
```

### HTML links

```text
<link type="application/pdf">
<link rel="alternate" ...>
<a>Download PDF</a>
<a>View PDF</a>
<a>Article PDF</a>
<a>Full text PDF</a>
```

### Embedded files

```text
<iframe>
<embed>
<object>
```

when type or URL evidence indicates PDF.

### JSON-LD（JSON 链接数据）

PDF media paths can be collected from structured media fields such as:

```text
contentUrl
downloadUrl
url
fileFormat / encodingFormat / contentType
```

### URL handling

- relative links resolve against the final page URL;
- `<base href>` is honored when usable;
- fragments are removed;
- non-HTTP schemes are ignored;
- duplicate routes merge evidence instead of creating duplicate candidates.

### Visible-text discipline

Page semantics are derived from visible page text, not arbitrary source-code strings.
Text inside ordinary `<script>`, `<style>` and `<template>` blocks is excluded from
`visible_text`; JSON-LD is still collected separately.

This prevents vendor JavaScript containing words such as `cloudflare`, `captcha` or
`access denied` from turning a normal article page into a false challenge page.

## 7. Page identity

Page identity is intentionally conservative:

```text
MATCH
MISMATCH
UNKNOWN
```

Strong positive evidence includes:

- target DOI in scholarly page metadata;
- very strong expected-title match when DOI metadata is absent.

A **different generic page title is not enough to emit MISMATCH**. Resolver bridges,
repository shells, access interstitials and challenge pages often use titles unrelated
to the paper.

`MISMATCH` requires explicit scholarly DOI metadata pointing to another work.

This policy was hardened after the real CDI corpus showed Elsevier LinkingHub and
challenge pages being incorrectly interpreted as wrong-paper pages when title conflict
alone was used.

## 8. Page semantics and precedence

The page classifier distinguishes:

```text
ARTICLE
LOGIN
ACCESS_DENIED
CHALLENGE
GENERIC
PDF_RESPONSE
UNKNOWN
```

Strong challenge/access-denied evidence describes the response actually served and has
priority over stale/copied scholarly metadata.

Identity and access are separate questions:

```text
"This is the requested paper"
≠
"This response currently grants file access"
```

Therefore explicit access-boundary language such as `Access through your institution`,
`Sign in to access`, `Purchase article` or `Subscribe to read` can classify a page as
`LOGIN` even when DOI metadata correctly identifies the target paper.

Conversely, weak/global navigation controls such as a generic `Get access` link do not
override a positively matched article page.

Resolution precedence is:

```text
CHALLENGE / ACCESS_DENIED → ACCESS_BLOCKED
explicit access boundary → AUTH_REQUIRED
explicit DOI mismatch     → PAGE_MISMATCH
otherwise                 → derive file candidates
```

This avoids false authentication claims, false paper mismatches, and the opposite error
of hiding a real authentication boundary behind correct paper metadata.

## 9. Document-role hints

Route Resolution uses:

```text
ARTICLE
SUPPLEMENT
UNKNOWN
```

as **hints**, not final document classifications.

Strong article metadata such as `citation_pdf_url` is an ARTICLE hint. Supporting
Information text/filenames become SUPPLEMENT hints.

Known auxiliary scientific PDFs such as:

```text
Reporting Summary
Peer Review file
Transparent Peer Review
Source Data
Decision Letter
Reviewer Comments
Author Checklist
```

remain `UNKNOWN` rather than being promoted to ARTICLE merely because the link also
contains “download PDF”.

Final authority remains v0.5.0 file validation. A role hint can improve ordering but
cannot create `VERIFIED` by itself.

## 10. HTTP → HTTPS route derivation

The CDI corpus exposed a real repository case where a provider-reported HTTP PDF route
failed while the corresponding HTTPS route was usable.

v0.5.1 therefore supports an explicit, traceable HTTPS alternative:

```text
http://host/path/file.pdf
↓
https://host/path/file.pdf
```

This is **derivation**, not normalization. The original route is preserved, and the new
candidate records:

```text
method = HTTPS_UPGRADE
parent_url = original HTTP route
```

AN does not globally rewrite arbitrary HTTP URLs and pretend that HTTP/HTTPS are always
equivalent.

## 11. Candidate ranking

Ranking answers:

> Which derived file route is most promising to hand to Acquisition next?

It is deterministic and evidence-based. Broadly:

```text
DIRECT_PDF_RESPONSE
>
citation_pdf_url
>
other PDF metadata
>
<link> PDF
>
JSON-LD PDF
>
semantic Download/View PDF anchor
>
embedded PDF
>
PDF-looking URL
>
HTTPS alternative
```

ARTICLE hints receive a modest bonus. SUPPLEMENT hints are strongly demoted. HTTPS
receives a small preference.

This is acquisition priority, not scholarly authority.

## 12. Fixed CDI real-network benchmark

v0.5.1 keeps the fixed corpus at:

```text
benchmarks/cdi_acquisition_10.json
```

Manual runner:

```text
scripts/manual_cdi_10_route_acceptance.py
```

The same ten papers span Springer Nature, ACS, Elsevier, Wiley, RSC, repositories,
open-access and access-restricted behavior. Difficult cases are intentionally retained
instead of being replaced with easier papers.

### Real-network acceptance history

Temporary GitHub Actions workflows ran the actual branch against this fixed corpus on
2026-09-16. They used OpenAlex Discovery only; no personal Unpaywall email was embedded
in CI. Temporary workflows were removed after acceptance so changing third-party sites
do not make normal CI flaky.

A hardened run before the final semantic audit observed:

```text
VERIFIED                                      5 / 10
NO_FILE_CANDIDATE                             3 / 10
ACCESS_BLOCKED                                1 / 10
INVALID_PDF | RETRIEVED_UNVERIFIED | SUPPLEMENT  1 / 10
```

Verified routes included:

- Nature Communications: landing page → `citation_pdf_url` → verified Nature PDF;
- npj Clean Water: existing direct Nature PDF remained verified;
- older Springer OA: obsolete provider PDF failed, but DOI landing page exposed a new
  Springer PDF route that verified;
- Elsevier OA: HAL landing page exposed a repository document that verified;
- Wiley OA: PolyU repository landing page exposed a usable HTTPS file route despite the
  provider HTTP PDF route failing.

### Final post-hardening rerun

After the final parser/access/streaming hardening, the exact same corpus was run again.
Observed paper-level outcomes were:

```text
VERIFIED            4 / 10
NO_FILE_CANDIDATE   5 / 10
ACCESS_BLOCKED      1 / 10
```

The four verified papers were npj Clean Water, the older Springer OA review, the
Elsevier/HAL OA review, and Wiley/PolyU. RSC remained explicitly `ACCESS_BLOCKED`.
Nature Water's resolver page was classified as `AUTH_REQUIRED` during route resolution.

The apparent 5/10 → 4/10 difference is external-network variability rather than evidence
of a lost parser capability: in the final run the Nature Communications case that had
previously verified received `ACCESS_BLOCKED` responses on all discovered landing/index/
repository routes from the GitHub runner.

This is why live-network `VERIFIED` count is evidence, but **not a deterministic release
gate**.

Across the hardened runs:

- generic resolver title conflict stayed conservative instead of becoming false mismatch;
- challenge pages became `ACCESS_BLOCKED / CHALLENGE`;
- explicit institution/login boundaries became `AUTH_REQUIRED` when the response said so;
- Nature Reporting Summary / Peer Review files were not promoted to ARTICLE;
- Supporting Information remained rejected;
- stale or broken provider PDF routes did not become false success;
- **no wrong file was promoted to `VERIFIED`.**

### Accepted remaining boundaries

- ACS/RSC and other sites can block automated clients despite public/OA content;
- a matched repository page can expose no file route in retrieved static HTML;
- JavaScript-only routes are outside v0.5.1;
- authenticated/CARSI/browser paths remain later work;
- external sites can change between otherwise identical benchmark runs.

These are intentionally **not** solved by guessed URL construction, publisher-specific
hardcoding, anti-bot bypass, or authentication circumvention.

## 13. Deterministic acceptance

Final deterministic CI after all release-audit hardening:

```text
Python 3.11  ✅
Python 3.14  ✅

editable install  ✅
pip check         ✅
Ruff format       ✅
Ruff static       ✅
compileall        ✅
pytest            ✅
```

The final suite contains **461 passing tests** on Python 3.11 and passes on Python 3.14
as well.

Final regression coverage includes real-network lessons for:

- generic resolver title conflict staying UNKNOWN;
- challenge/access semantics taking precedence over misleading page titles;
- stale scholarly metadata on challenge pages;
- script/style/template text not contaminating visible page semantics;
- explicit authentication boundaries taking precedence over matching paper identity;
- weak/global access navigation not overriding a positively matched article;
- auxiliary Nature-style PDFs not being marked ARTICLE;
- Supporting Information hints;
- PDF sniffing before rejecting uncertain large binary responses;
- bounded 64 KiB read chunks when a landing route directly returns a PDF.

## 14. Accepted non-goals

v0.5.1 intentionally does not implement:

- automatic multi-candidate orchestration;
- browser JavaScript execution;
- CARSI / SSO / institutional login;
- cookie/session persistence;
- CAPTCHA or anti-bot circumvention;
- paywall bypass;
- a publisher-specific adapter catalogue;
- guessed publisher PDF URL synthesis;
- OCR or scientific-content parsing;
- a literature database.

These exclusions are architectural boundaries, not hidden failures.

## 15. Why no further mechanisms are added to v0.5.1

The fixed corpus and release audit do not provide evidence that another generic parser
primitive would materially improve this layer without crossing its boundary.

The remaining failures are dominated by access blocking/authentication, JavaScript-only
behavior, third-party network variability, or pages that simply do not expose a file in
the retrieved static HTML. Adding speculative meta-refresh rules, publisher URL
templates, browser automation, or guessed repository paths solely to improve a benchmark
score would violate the project principles:

> solve real problems, preserve provenance, prefer explicit failure, and do not pay
> complexity cost before the requirement is demonstrated.

v0.5.1 is therefore frozen here.

## 16. Next layer

v0.5.2 should build **Multi-route Acquisition（多路径获取编排）** on the stable primitives:

```text
Discovery Candidate
        ↓
Route Resolution when needed
        ↓
Direct PDF Acquisition + Validation
        ↓
try next route only when policy permits
        ↓
first VERIFIED article stops
```

That layer will own route-attempt policy, fallback, stop conditions and complete attempt
history. v0.5.1 remains a stable, independently testable route-to-candidate capability.

**Status: FINALIZED / FROZEN FOR MERGE.**
